{
  "name": "baccarat-simulator",
  "private": true,
  "workspaces": ["web", "smoke"],
  "scripts": {
    "build:wasm": "wasm-pack build engine-wasm --target bundler",
    "phones": "node scripts/phones.mjs"
  }
}
